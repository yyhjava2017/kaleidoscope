server:
  port: 9000
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/yinydb?characterEncoding=utf-8&useSSL=false
    driverClassName: com.mysql.jdbc.Driver
    username: root
    password: root
mybatis:
  mapper-locations: classpath:com/user/dao/*.xml

logging:
  path: d:/log
  level:
    root: info
